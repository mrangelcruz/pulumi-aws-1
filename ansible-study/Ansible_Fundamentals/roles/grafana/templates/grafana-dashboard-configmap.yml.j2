apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboard
  namespace: {{ grafana_namespace }}
  labels:
    grafana_dashboard: "1"
data:
  node-exporter-dashboard.json: |
    {{ lookup('file', 'files/node-exporter-dashboard.json') | indent(4) }}
